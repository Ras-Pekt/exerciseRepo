#include <stdio.h>
/**
 * main - prints 3 characters (B, y, and e) to stdout
 * Return: 0 on success
 */
int main(void)
{
	putc('B', stdout);
	putc('y', stdout);
	putc('e', stdout);
	putc('\n', stdout);
	
	return (0);
}
