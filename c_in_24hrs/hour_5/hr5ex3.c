#include <stdio.h>

/**
 * main - Displays the base-10 numbers in Hex
 * Return: 0 on success
 */
int main(void)
{
	int x = 15, y = 150, z = 1500;

	printf("15 in Hex = %X\n150 in Hex = %X\n1500 in Hex = %X\n", x, y, z);

	return(0);
}
